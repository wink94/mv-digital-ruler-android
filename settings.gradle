pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "mv-cpp-android-digital-ruler"
include ':app'
include ':opencv'
project(':opencv').projectDir = new File(opencvsdk + '/sdk')
